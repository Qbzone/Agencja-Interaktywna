@model List<Agencja_Interaktywna.Models.Projekt>
@{
    ViewData["Title"] = "Index";
}

<h1>Witaj @ViewBag.userEmail </h1>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Logo projektu</th>
            <th scope="col">Nazwa projektu</th>
            <th scope="col">Podgląd</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in @Model)
        {
            <tr>
                <td>
                    <img src="/@item.Logo" alt="/@item.Logo" height="100" width="100" />
                </td>
                <td>
                    @item.Nazwa
                </td>

                <td>
                    <a asp-action="ProjectDetails" asp-route-id="@item.IdProjekt" class="btn btn-primary">Podgląd</a>
                </td>
            </tr>
        }
    </tbody>
</table>