@model List<Interactive_Agency.Models.Team>
@{
    ViewData["Title"] = "Teams";
}

<h1>Zespoły</h1>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Nazwa zespołu</th>
            <th scope="col">Akcje</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in @Model)
        {
            item.View = "Teams";
            <tr>
                <td>
                    @item.TeamName
                </td>
                <td>
                    <a asp-action="Team" asp-route-id="@item.TeamId" asp-route-view="@item.View" class="btn btn-primary">Wyświetl zespół</a>
                </td>
            </tr>
        }
    </tbody>
</table>